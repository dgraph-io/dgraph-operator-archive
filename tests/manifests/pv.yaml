apiVersion: v1
kind: PersistentVolume
metadata:
  name: dgraph-test-pv-volume
  labels:
    type: local
spec:
  storageClassName: standard
  capacity:
    storage: 15Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/mnt/data"
